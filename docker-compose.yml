services:
  telegram:
    build: ./telegram
    ports:
      - "8005:8005"
    volumes:
      - ./telegram/config:/app/config
      - ./telegram/sessions:/app/sessions
    environment:
      - NODE_ENV=production
      - PORT=8005
    restart: unless-stopped
